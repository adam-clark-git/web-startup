import React, { useContext, useState } from 'react';
export function Join(gameID)
{
    return (
        <div>
            <p> You have joined a game! Please wait until the host declares it ready.</p>
        </div>
    );
}